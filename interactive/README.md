# Interactive Artifacts

Static public [T]-Theory artifacts. GitHub Pages publishes this directory only;
PDF distribution, Zenodo files, and print artifacts remain outside the web
deployment.

- `soma-field-operator/`: interactive 11D body map. The visual engine is
  sourced from the pinned U commit and Three.js is loaded from a pinned CDN.